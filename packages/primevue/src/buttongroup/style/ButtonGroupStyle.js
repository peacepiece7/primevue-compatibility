import { style } from '@peacepieceuix-compatibility/styles/buttongroup';
import BaseStyle from '@peacepiece-compatibility/core/base/style';

const classes = {
    root: 'p-buttongroup p-component'
};

export default BaseStyle.extend({
    name: 'buttongroup',
    style,
    classes
});
