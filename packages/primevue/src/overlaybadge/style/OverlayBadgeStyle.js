import { style } from '@peacepieceuix-compatibility/styles/overlaybadge';
import BaseStyle from '@peacepiece-compatibility/core/base/style';

const classes = {
    root: 'p-overlaybadge'
};

export default BaseStyle.extend({
    name: 'overlaybadge',
    style,
    classes
});
