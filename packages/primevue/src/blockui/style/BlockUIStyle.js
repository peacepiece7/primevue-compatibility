import { style } from '@peacepieceuix-compatibility/styles/blockui';
import BaseStyle from '@peacepiece-compatibility/core/base/style';

const classes = {
    root: 'p-blockui'
};

export default BaseStyle.extend({
    name: 'blockui',
    style,
    classes
});
