import BaseDirective from '@peacepiece-compatibility/core/basedirective';
import KeyFilterStyle from 'peacepiece-primevue/keyfilter/style';

const BaseKeyFilter = BaseDirective.extend({
    style: KeyFilterStyle
});

export default BaseKeyFilter;
